{
  "name": "functions",
  "description": "Serverless functions for shokri.dev using AWS lambda",
  "scripts": {
    "deploy": "serverless deploy --stage production",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:api": "serverless deploy function --function api --stage production"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.145.0",
    "cors": "^2.8.5",
    "dompurify": "^2.3.10",
    "express": "^4.18.1",
    "helmet": "^5.1.1",
    "jsdom": "^20.0.0",
    "serverless-http": "^3.0.2"
  },
  "devDependencies": {
    "eslint": "^8.21.0",
    "eslint-plugin-promise": "^6.0.0"
  },
  "engines": {
    "node": "^14.0.0"
  },
  "private": true
}
